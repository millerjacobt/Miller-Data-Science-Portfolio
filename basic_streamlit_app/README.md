### V-DEM Core Indices Visualization Tool 

This is a basic streamlit app that allows users to select a country of interest and view the data from indices over a given time period. The 5 core indices are listed above each plot with a short description of what they capture. 

## How to Run the App

From the root of the repository, run:

```bash
streamlit run basic_streamlit_app/main.py